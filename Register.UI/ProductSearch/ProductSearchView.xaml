<UserControl x:Class="Register.ProductSearch.ProductSearchView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:productSearch="clr-namespace:Register.ProductSearch"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance productSearch:ProductSearchVM}"
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <ContentControl Content="{Binding Path=Products}" />
    </Grid>
</UserControl>